[tool.poetry]
name = "filters"
version = "0.1.0"
description = ""
authors = ["Sebastien Pfister <sebastien.pfister@heig-vd.ch>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
